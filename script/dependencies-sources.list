#[apt] Installation commands:
  sudo -H apt-get source --download-only python-defusedxml
  sudo -H apt-get source --download-only ros-kinetic-rosconsole
  sudo -H apt-get source --download-only ros-kinetic-rosparam
  sudo -H apt-get source --download-only ros-kinetic-catkin
  sudo -H apt-get source --download-only ros-kinetic-rostest
  sudo -H apt-get source --download-only libapr1-dev
  sudo -H apt-get source --download-only libaprutil1-dev
  sudo -H apt-get source --download-only ros-kinetic-topic-tools
  sudo -H apt-get source --download-only ros-kinetic-rosgraph
  sudo -H apt-get source --download-only ros-kinetic-genpy
  sudo -H apt-get source --download-only ros-kinetic-gennodejs
  sudo -H apt-get source --download-only ros-kinetic-cmake-modules
  sudo -H apt-get source --download-only ros-kinetic-roslisp
  sudo -H apt-get source --download-only python-nose
  sudo -H apt-get source --download-only ros-kinetic-rosservice
  sudo -H apt-get source --download-only ros-kinetic-rosout
  sudo -H apt-get source --download-only python-numpy
  sudo -H apt-get source --download-only ros-kinetic-rospack
  sudo -H apt-get source --download-only ros-kinetic-ros
  sudo -H apt-get source --download-only python-paramiko
  sudo -H apt-get source --download-only ros-kinetic-rosmake
  sudo -H apt-get source --download-only libbz2-dev
  sudo -H apt-get source --download-only cmake
  sudo -H apt-get source --download-only ros-kinetic-roscpp
  sudo -H apt-get source --download-only python-empy
  sudo -H apt-get source --download-only python-netifaces
  sudo -H apt-get source --download-only ros-kinetic-roslib
  sudo -H apt-get source --download-only ros-kinetic-geneus
  sudo -H apt-get source --download-only ros-kinetic-roscpp-serialization
  sudo -H apt-get source --download-only ros-kinetic-rosbuild
  sudo -H apt-get source --download-only libgtest-dev
  sudo -H apt-get source --download-only sbcl
  sudo -H apt-get source --download-only python-coverage
  sudo -H apt-get source --download-only ros-kinetic-message-generation
  sudo -H apt-get source --download-only google-mock
  sudo -H apt-get source --download-only libboost-all-dev
  sudo -H apt-get source --download-only liblog4cxx10-dev
  sudo -H apt-get source --download-only ros-kinetic-rostopic
  sudo -H apt-get source --download-only ros-kinetic-genlisp
  sudo -H apt-get source --download-only ros-kinetic-ros-environment
  sudo -H apt-get source --download-only python-mock
  sudo -H apt-get source --download-only ros-kinetic-message-runtime
  sudo -H apt-get source --download-only ros-kinetic-message-filters
  sudo -H apt-get source --download-only python-yaml
  sudo -H apt-get source --download-only python-dev
  sudo -H apt-get source --download-only ros-kinetic-std-msgs
  sudo -H apt-get source --download-only ros-kinetic-roscpp-traits
  sudo -H apt-get source --download-only ros-kinetic-rostime
  sudo -H apt-get source --download-only ros-kinetic-roscreate
  sudo -H apt-get source --download-only liblz4-dev
  sudo -H apt-get source --download-only python-rospkg
  sudo -H apt-get source --download-only ros-kinetic-roswtf
  sudo -H apt-get source --download-only ros-kinetic-genmsg
  sudo -H apt-get source --download-only ros-kinetic-rospy
  sudo -H apt-get source --download-only ros-kinetic-rosunit
  sudo -H apt-get source --download-only libtinyxml-dev
  sudo -H apt-get source --download-only libconsole-bridge-dev
  sudo -H apt-get source --download-only ros-kinetic-rosbag-storage
  sudo -H apt-get source --download-only ros-kinetic-rosbash
  sudo -H apt-get source --download-only ros-kinetic-roslaunch
  sudo -H apt-get source --download-only ros-kinetic-roslang
  sudo -H apt-get source --download-only ros-kinetic-rosboost-cfg
  sudo -H apt-get source --download-only ros-kinetic-cpp-common
  sudo -H apt-get source --download-only ros-kinetic-std-srvs
  sudo -H apt-get source --download-only ros-kinetic-xmlrpcpp
  sudo -H apt-get source --download-only python-imaging
  sudo -H apt-get source --download-only ros-kinetic-gencpp
  sudo -H apt-get source --download-only ros-kinetic-mk
  sudo -H apt-get source --download-only ros-kinetic-roslz4
  sudo -H apt-get source --download-only python-catkin-pkg
  sudo -H apt-get source --download-only ros-kinetic-rosmaster
  sudo -H apt-get source --download-only ros-kinetic-rosgraph-msgs
  sudo -H apt-get source --download-only ros-kinetic-rosmsg
  sudo -H apt-get source --download-only python-rosdep
  sudo -H apt-get source --download-only ros-kinetic-rosnode
  sudo -H apt-get source --download-only ros-kinetic-rosbag
  sudo -H apt-get source --download-only ros-kinetic-rosclean
  sudo -H apt-get source --download-only pkg-config
